module()

bazel_dep(name = "rules_go", version = "0.41.0")
bazel_dep(name = "genrules", version = "0.1")

# local_path_override(
#     module_name = "genrules",
#     path = "../genrules",
# )

# TODO: toolchainify
deps = use_extension("//:deps.bzl", "deps")
use_repo(deps, "crane", "gcloud")
