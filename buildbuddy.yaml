actions:
  - name: "Deploy"
    triggers:
      push:
        branches:
          - "main"
    bazel_commands:
      - "run server:auth --config=workflows"
      - "run server:deploy --config=workflows"
